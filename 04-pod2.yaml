apiVersion: v1
kind: Pod
metadata:
  name: frontend2
  namespace: roboshop
  labels:
    app: frontend
    version: v2
    environment: production
    tier: web
spec:
  containers:
  - name: frontend
    image: nginx